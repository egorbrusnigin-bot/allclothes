# ALLCLOTHES (fixed)

Every header icon and every account sidebar item is clickable and has its own page.

## Run
```bash
npm install
npm run dev
```

## Supabase (optional)
Create `.env.local` from `.env.example` and paste:
- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY
