# Обновления системы аутентификации и админ-панели

## Что было сделано:

### 1. Новая таблица в базе данных
Создана таблица **`sellers`** для хранения информации о продавцах:
- Связь с пользователем через `user_id`
- Информация о бренде (название, логотип, описание)
- Тип продавца (brand/store/creator)
- Контактные данные
- Информация о выплатах

### 2. Измененная система регистрации
**До:**
- Модальное окно для входа и регистрации
- Сразу создавался аккаунт при клике "Create account"

**После:**
- Модальное окно только для входа
- Кнопка "Create account" → перенаправляет на `/signup`
- На странице `/signup`:
  - Шаг 1: Выбор типа аккаунта (Buyer / Seller)
  - Шаг 2: Email + Password
  - Seller → перенаправление на `/account/become-seller`
  - Buyer → перенаправление на главную

### 3. Админ-панель перемещена в личный кабинет
**До:**
- Доступна по адресу `/admin`
- Открыта для всех
- Интерфейс на русском языке

**После:**
- Доступна по адресу `/account/admin`
- Только для продавцов (проверка в таблице `sellers`)
- Интерфейс полностью на английском
- Непродавцам показывается сообщение с предложением стать продавцом

### 4. Обновленная навигация в личном кабинете
В sidebar добавлен пункт **"Admin Panel"**:
- Показывается только для пользователей со статусом продавца
- Расположен после "Become a seller"
- Динамически подгружается при проверке статуса

### 5. Форма "Become a seller" теперь функциональна
**До:**
- Форма собирала данные, но не сохраняла их

**После:**
- Сохраняет все данные в таблицу `sellers`
- Загружает логотип в Supabase Storage (`brand-logos`)
- После успешного сохранения:
  - Если уже продавец → показывает сообщение + кнопку "Go to Admin Panel"
  - После создания → перенаправляет в админ-панель
- Все поля формы работают корректно

### 6. Убраны публичные ссылки на админку
**До:**
- На главной странице: "Добавить первый бренд в админке"
- На странице бренда: "Добавить товар в админке"

**После:**
- Главная страница: простой текст "No brands yet"
- Страница бренда: "No products yet for this brand"
- Никаких прямых ссылок на админ-функции

### 7. Весь интерфейс на английском
Переведены:
- Все сообщения об ошибках
- Alert'ы и уведомления
- Labels и placeholders в формах
- Комментарии в коде
- Кнопки и навигация

---

## Как применить изменения:

### Шаг 1: Обновить базу данных
1. Открой Supabase консоль: https://mvsaxxnlnzpswtyjpgkr.supabase.co
2. Перейди в **SQL Editor**
3. Открой файл `supabase-setup.sql`
4. Скопируй **ТОЛЬКО НОВУЮ ЧАСТЬ** (секцию с таблицей sellers):

```sql
-- ====================
-- ТАБЛИЦА ПРОДАВЦОВ (SELLERS)
-- ====================
CREATE TABLE IF NOT EXISTS sellers (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  brand_name TEXT NOT NULL,
  logo_url TEXT,
  description TEXT,
  seller_type TEXT, -- 'brand', 'store', 'creator'
  contact_email TEXT,
  shopify_link TEXT,
  telegram TEXT,
  instagram TEXT,
  paypal_email TEXT,
  legal_name TEXT,
  status TEXT DEFAULT 'pending', -- 'pending', 'approved', 'rejected'
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL,
  UNIQUE(user_id)
);

-- Включаем ROW LEVEL SECURITY
ALTER TABLE sellers ENABLE ROW LEVEL SECURITY;

-- ====================
-- ПОЛИТИКИ ДЛЯ SELLERS
-- ====================
CREATE POLICY "Users can view own seller profile"
  ON sellers FOR SELECT
  USING (auth.uid() = user_id);

CREATE POLICY "Users can create own seller profile"
  ON sellers FOR INSERT
  WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update own seller profile"
  ON sellers FOR UPDATE
  USING (auth.uid() = user_id);
```

5. Вставь в SQL Editor и нажми **RUN**

### Шаг 2: Перезапусти проект
```bash
npm run dev
```

### Шаг 3: Проверь работу
1. Открой http://localhost:3000
2. Нажми LOGIN → "Create account"
3. Выбери тип аккаунта
4. Зарегистрируйся
5. Если выбрал "Seller" → заполни форму "Become a seller"
6. Проверь, что в sidebar появилась ссылка "Admin Panel"
7. Зайди в админ-панель и попробуй создать бренд

---

## Структура файлов:

```
app/
├── components/
│   └── AuthModal.tsx              ← Обновлен (только вход)
├── signup/
│   └── page.tsx                   ← НОВЫЙ (регистрация с выбором типа)
├── account/
│   ├── sidebar.tsx                ← Обновлен (условная ссылка на админку)
│   ├── become-seller/
│   │   └── page.tsx               ← Обновлен (сохранение в sellers)
│   └── admin/
│       └── page.tsx               ← ПЕРЕМЕЩЕН из /admin (на английском)
├── page.tsx                       ← Убраны ссылки на админку
└── brand/[slug]/page.tsx          ← Убраны ссылки на админку

supabase-setup.sql                 ← Добавлена таблица sellers
```

---

## Важно:

### Проверка статуса продавца
Система проверяет наличие записи в таблице `sellers` для текущего пользователя:
```typescript
const { data } = await supabase
  .from("sellers")
  .select("id")
  .eq("user_id", user.id)
  .single();

if (data) {
  // Пользователь - продавец
  // Показываем Admin Panel в sidebar
  // Даем доступ к /account/admin
}
```

### Безопасность
- Row Level Security (RLS) включен для таблицы `sellers`
- Пользователи могут видеть и редактировать только свой профиль продавца
- Админ-панель проверяет статус продавца перед отображением
- Старый `/admin` route удален

### Что дальше?
После этих изменений ты можешь:
- Зарегистрировать тестового продавца
- Создать бренды через админ-панель
- Добавить товары к брендам
- Все это будет видно на главной странице

---

## Если что-то не работает:

1. **Ошибка "table sellers does not exist"**
   - Выполни SQL-скрипт создания таблицы в Supabase

2. **Админ-панель не показывается в sidebar**
   - Убедись, что ты заполнил форму "Become a seller"
   - Проверь в Supabase → Table Editor → sellers, есть ли там запись для твоего user_id

3. **Ошибка при загрузке логотипа**
   - Проверь, что bucket `brand-logos` создан в Storage
   - Проверь политики доступа к bucket

4. **"Supabase not configured"**
   - Проверь файл `.env.local`
   - Убедись, что переменные `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_ANON_KEY` заполнены

---

Готово! 🎉
Теперь у тебя полноценная система с регистрацией, проверкой статуса продавца и защищенной админ-панелью.
